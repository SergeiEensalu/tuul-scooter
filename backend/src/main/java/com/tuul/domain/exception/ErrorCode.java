package com.tuul.domain.exception;

// Comment by Sergei Eensalu: probably these errors can be splitted by category. But not this time.
public enum ErrorCode {
    USER_ALREADY_EXISTS,
    INVALID_CREDENTIALS,
    VALIDATION_ERROR,
    VEHICLE_NOT_FOUND,
    VEHICLE_ALREADY_RESERVED
}

